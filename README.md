# tiketing
- Download via git atau download zip
- extak file project (tiket) ke folder web server jika laragon (wwww) , xampp (htdoct), iis (inet/pub) , atau lain nya
- import database dengan name "Tiket" (sql server 2017)
- Setting database di file aplication/config/database.php
- jalankan localhost/tiketing atau localhost:port (masing masing webserver)
  
